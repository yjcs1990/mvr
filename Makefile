####
#### MVR mobile vacuum cleaner
#### Mvria top level Makefile
####

# Make sure we're using a compiler (we probably don't need to define it
# but we have been so here it is unless it already set)
ifndef CXX
CXX:=g++
endif

###
### General variables
###
